inst/doc
.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store

# pkgdown site — generated locally via build_site(); the live site is
# deployed separately (rpkgs.datanovia.com), so the build artifact is not tracked.
docs/

# Local Claude Code config + maintainer skill bundle (synced from r-pkg-maintainer-kit)
.claude/
CLAUDE.md

# gh-aw local run logs (generated by `gh aw run/audit`)
.github/aw/logs/
*.tar.gz
rstatix.Rcheck/
